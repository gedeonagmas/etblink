const emailTemplate = (subject, message) => {
  return `<!DOCTYPE html>
  <html>
      <head>
          <title>Basic Web Page</title>
      </head>
      <body>
          Hello World!
      </body>
  </html>`;
};

module.exports = { emailTemplate };
